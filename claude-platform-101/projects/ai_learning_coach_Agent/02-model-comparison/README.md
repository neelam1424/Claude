# Claude Model Comparison Tool

## Project Overview

This project is part of **Claude Platform 101 — Module 3: Choosing The Right Model**.

The goal of this project is to understand how different Claude models behave when given the same user prompt.

The user enters a topic, and the program sends that same topic to multiple Claude models:

* Haiku
* Sonnet
* Opus

Then the program compares:

* Response quality
* Response speed
* Input token usage
* Output token usage
* Total token usage

This is an important real-world AI engineering skill because companies do not always use the most powerful model. They choose the right model based on cost, speed, and task complexity.

---

## What This Project Teaches

This project teaches:

* How to call multiple Claude models
* How to compare model responses
* How to measure response speed
* How to track token usage
* How to think about cost optimization
* How to choose the right model for the right task
* How real AI applications balance quality, speed, and price

---

## Project Folder

```bash
02-model-comparison
```

---

## Tech Stack

* Python
* Anthropic SDK
* Claude Messages API
* python-dotenv
* CLI application

---

## How The Project Works

The user enters a topic:

```text
Deep Learning
```

The program sends this same prompt to three Claude models.

Each model explains the topic.

The program records:

1. Which model was used
2. How long the response took
3. How many input tokens were used
4. How many output tokens were used
5. Total tokens used
6. The actual response from Claude

At the end, the program prints a comparison summary.

---

## Important Concepts

### 1. Model Selection

Model selection means choosing the correct AI model for the task.

In real AI applications, we usually do not use the strongest model for everything.

Example:

| Task                | Best Model Choice |
| ------------------- | ----------------- |
| Simple summary      | Haiku             |
| Normal chatbot      | Sonnet            |
| Complex reasoning   | Opus              |
| Fast classification | Haiku             |
| Coding assistant    | Sonnet or Opus    |
| Deep research       | Opus              |

The goal is to balance:

```text
Quality + Speed + Cost
```

---

### 2. Latency

Latency means how long the model takes to respond.

In this project, latency is measured using Python's `time` module.

```python
start_time = time.time()
```

This records the time before the API call.

```python
end_time = time.time()
```

This records the time after the API call.

Then:

```python
response_time = round(end_time - start_time, 2)
```

This gives the response time in seconds.

Lower response time means the model is faster.

---

### 3. Token Usage

AI models do not count cost by words. They count cost by tokens.

A token is a small piece of text.

Example:

```text
Deep Learning is powerful
```

may be split into multiple tokens.

The Claude API returns token usage inside:

```python
response.usage
```

We use:

```python
response.usage.input_tokens
```

for the tokens sent to Claude.

We use:

```python
response.usage.output_tokens
```

for the tokens Claude generated.

Total tokens:

```python
total_tokens = input_tokens + output_tokens
```

More tokens usually means higher cost.

---

### 4. Input Tokens

Input tokens are tokens from:

* System prompt
* User prompt
* Conversation history

Example:

```python
system="You are a beginner-friendly AI learning coach."
```

and:

```python
"Explain Deep Learning in simple beginner-friendly language."
```

Both count as input tokens.

---

### 5. Output Tokens

Output tokens are the tokens generated by Claude.

Example:

```text
Deep Learning is a type of machine learning...
```

This response counts as output tokens.

Longer answers use more output tokens.

---

### 6. Cost Optimization

Cost optimization means reducing unnecessary API cost while still getting good results.

Example:

If Haiku gives a good enough answer, there is no need to use Opus.

A good production system might use:

```text
Haiku for simple tasks
Sonnet for normal tasks
Opus for hard tasks
```

This saves money and improves speed.

---

## Project Code

```python
import os
import time
from dotenv import load_dotenv
from anthropic import Anthropic, APIError, BadRequestError

load_dotenv()

client = Anthropic(api_key=os.getenv("ANTHROPIC_API_KEY"))

MODELS = {
    "Haiku": "claude-haiku-4-5",
    "Sonnet": "claude-sonnet-4-6",
    "Opus": "claude-opus-4-8",
}


def ask_claude(model_name: str, model_id: str, topic: str) -> dict:
    prompt = f"""
Explain {topic} in simple beginner-friendly language.

Your answer should include:
1. Simple explanation
2. Real-life example
3. Why it matters
Keep the answer under 200 words.
"""

    start_time = time.time()

    response = client.messages.create(
        model=model_id,
        max_tokens=500,
        system="You are a beginner-friendly AI learning coach.",
        messages=[
            {
                "role": "user",
                "content": prompt,
            }
        ],
    )

    end_time = time.time()
    response_time = round(end_time - start_time, 2)

    output_text = response.content[0].text

    return {
        "model_name": model_name,
        "model_id": model_id,
        "response": output_text,
        "response_time": response_time,
        "input_tokens": response.usage.input_tokens,
        "output_tokens": response.usage.output_tokens,
        "total_tokens": response.usage.input_tokens + response.usage.output_tokens,
    }


def print_result(result: dict):
    print("\n" + "=" * 70)
    print(f"MODEL: {result['model_name']} ({result['model_id']})")
    print("=" * 70)

    print("\nClaude Response:\n")
    print(result["response"])

    print("\nPerformance:")
    print(f"Response Speed: {result['response_time']} seconds")
    print(f"Input Tokens: {result['input_tokens']}")
    print(f"Output Tokens: {result['output_tokens']}")
    print(f"Total Tokens: {result['total_tokens']}")


def compare_models(topic: str):
    results = []

    for model_name, model_id in MODELS.items():
        try:
            print(f"\nRunning {model_name}...")
            result = ask_claude(model_name, model_id, topic)
            results.append(result)
            print_result(result)

        except BadRequestError as error:
            print(f"\n{model_name} failed.")
            print("Reason:", error)

        except APIError as error:
            print(f"\nAPI error while running {model_name}.")
            print("Reason:", error)

        except Exception as error:
            print(f"\nUnexpected error while running {model_name}.")
            print("Reason:", error)

    if results:
        print_summary(results)


def print_summary(results: list):
    print("\n" + "#" * 70)
    print("MODEL COMPARISON SUMMARY")
    print("#" * 70)

    fastest = min(results, key=lambda x: x["response_time"])
    cheapest_tokens = min(results, key=lambda x: x["total_tokens"])
    longest_answer = max(results, key=lambda x: x["output_tokens"])

    print(f"\nFastest Model: {fastest['model_name']} - {fastest['response_time']} seconds")
    print(f"Lowest Token Usage: {cheapest_tokens['model_name']} - {cheapest_tokens['total_tokens']} tokens")
    print(f"Longest Answer: {longest_answer['model_name']} - {longest_answer['output_tokens']} output tokens")

    print("\nIndustry Meaning:")
    print("Haiku: Best for fast and cheaper simple tasks.")
    print("Sonnet: Best default choice for balanced quality and speed.")
    print("Opus: Best for complex reasoning, deeper explanations, and advanced tasks.")


def main():
    print("Claude Model Comparison Tool")
    print("----------------------------")

    topic = input("Enter a topic to explain: ")

    if not topic.strip():
        print("Please enter a valid topic.")
        return

    compare_models(topic)


if __name__ == "__main__":
    main()
```

---

## Code Explanation

### Importing Required Libraries

```python
import os
import time
```

`os` is used to read environment variables.

`time` is used to measure response speed.

```python
from dotenv import load_dotenv
```

This loads values from the `.env` file.

```python
from anthropic import Anthropic, APIError, BadRequestError
```

This imports the Anthropic SDK and error classes.

---

### Loading API Key

```python
load_dotenv()
```

This loads the `.env` file.

```python
client = Anthropic(api_key=os.getenv("ANTHROPIC_API_KEY"))
```

This creates the Anthropic client using the API key.

The API key should be stored inside `.env`:

```env
ANTHROPIC_API_KEY=your_api_key_here
```

Never push `.env` to GitHub.

---

### Models Dictionary

```python
MODELS = {
    "Haiku": "claude-haiku-4-5",
    "Sonnet": "claude-sonnet-4-6",
    "Opus": "claude-opus-4-8",
}
```

This dictionary stores model names and model IDs.

The left side is the friendly name.

The right side is the actual model ID used by the API.

Example:

```python
"Haiku": "claude-haiku-4-5"
```

means:

```text
Friendly name: Haiku
API model ID: claude-haiku-4-5
```

---

### ask_claude Function

```python
def ask_claude(model_name: str, model_id: str, topic: str) -> dict:
```

This function sends one request to one Claude model.

It receives:

* model name
* model ID
* user topic

It returns a dictionary containing:

* response text
* response time
* token usage
* model information

---

### Prompt Creation

```python
prompt = f"""
Explain {topic} in simple beginner-friendly language.

Your answer should include:
1. Simple explanation
2. Real-life example
3. Why it matters
Keep the answer under 200 words.
"""
```

This creates the prompt dynamically.

If the user enters:

```text
Deep Learning
```

then the final prompt becomes:

```text
Explain Deep Learning in simple beginner-friendly language.
```

---

### Measuring Start Time

```python
start_time = time.time()
```

This records the time before sending the API request.

---

### Calling Claude API

```python
response = client.messages.create(
    model=model_id,
    max_tokens=500,
    system="You are a beginner-friendly AI learning coach.",
    messages=[
        {
            "role": "user",
            "content": prompt,
        }
    ],
)
```

This sends the request to Claude.

#### `model=model_id`

This tells Claude which model to use.

Example:

```python
model="claude-sonnet-4-6"
```

#### `max_tokens=500`

This limits the maximum size of Claude's response.

#### `system=...`

This tells Claude how to behave.

Here, Claude behaves like a beginner-friendly learning coach.

#### `messages=[...]`

This contains the actual user message.

```python
{
    "role": "user",
    "content": prompt,
}
```

This means the user is asking Claude to explain the topic.

---

### Measuring End Time

```python
end_time = time.time()
```

This records the time after Claude returns a response.

```python
response_time = round(end_time - start_time, 2)
```

This calculates how many seconds the model took.

---

### Extracting Claude Response

```python
output_text = response.content[0].text
```

Claude's answer is inside:

```python
response.content
```

We use:

```python
[0].text
```

to get the first text response.

---

### Returning Results

```python
return {
    "model_name": model_name,
    "model_id": model_id,
    "response": output_text,
    "response_time": response_time,
    "input_tokens": response.usage.input_tokens,
    "output_tokens": response.usage.output_tokens,
    "total_tokens": response.usage.input_tokens + response.usage.output_tokens,
}
```

This returns all useful comparison data.

---

### print_result Function

```python
def print_result(result: dict):
```

This function prints the result for one model.

It displays:

* model name
* model ID
* Claude response
* response speed
* token usage

---

### compare_models Function

```python
def compare_models(topic: str):
```

This function runs all models one by one.

```python
for model_name, model_id in MODELS.items():
```

This loop goes through every model in the `MODELS` dictionary.

For each model:

```python
result = ask_claude(model_name, model_id, topic)
```

It calls Claude.

Then:

```python
results.append(result)
```

It saves the result.

Then:

```python
print_result(result)
```

It prints the output.

---

### Error Handling

```python
except BadRequestError as error:
```

This catches bad request errors.

Example:

* wrong model ID
* low credit balance
* invalid request format

```python
except APIError as error:
```

This catches general Anthropic API errors.

```python
except Exception as error:
```

This catches unexpected errors.

Error handling is important because in real production apps, API calls can fail.

---

### print_summary Function

```python
def print_summary(results: list):
```

This function compares all model results.

---

### Fastest Model

```python
fastest = min(results, key=lambda x: x["response_time"])
```

This finds the model with the lowest response time.

---

### Lowest Token Usage

```python
cheapest_tokens = min(results, key=lambda x: x["total_tokens"])
```

This finds the model that used the fewest total tokens.

Fewer tokens usually means lower cost.

---

### Longest Answer

```python
longest_answer = max(results, key=lambda x: x["output_tokens"])
```

This finds which model gave the longest response.

Longer does not always mean better, but it helps compare response depth.

---

### main Function

```python
def main():
```

This is the starting point of the program.

```python
topic = input("Enter a topic to explain: ")
```

This takes user input.

```python
if not topic.strip():
```

This checks if the user entered an empty topic.

```python
compare_models(topic)
```

This starts the model comparison.

---

### Program Entry Point

```python
if __name__ == "__main__":
    main()
```

This means:

Run `main()` only when this file is executed directly.

Example:

```bash
python main.py
```

---

## How To Run The Project

### Step 1: Create virtual environment

```bash
python3 -m venv venv
```

### Step 2: Activate virtual environment

Mac/Linux:

```bash
source venv/bin/activate
```

Windows:

```bash
venv\Scripts\activate
```

### Step 3: Install dependencies

```bash
pip install -r requirements.txt
```

### Step 4: Add API key

Create `.env`:

```env
ANTHROPIC_API_KEY=your_api_key_here
```

### Step 5: Run

```bash
python main.py
```

---

## Example Usage

```text
Claude Model Comparison Tool
----------------------------
Enter a topic to explain: Deep Learning
```

Example output:

```text
Running Haiku...

MODEL: Haiku
Response Speed: 1.25 seconds
Input Tokens: 50
Output Tokens: 150
Total Tokens: 200

Running Sonnet...

MODEL: Sonnet
Response Speed: 2.10 seconds
Input Tokens: 50
Output Tokens: 180
Total Tokens: 230

Running Opus...

MODEL: Opus
Response Speed: 4.50 seconds
Input Tokens: 50
Output Tokens: 220
Total Tokens: 270
```

---

## Industry Interpretation

### Haiku

Haiku is useful when:

* You need fast responses
* The task is simple
* You want lower cost
* You are doing classification, summaries, or short answers

### Sonnet

Sonnet is useful when:

* You want a balance between speed and intelligence
* You are building most real-world AI apps
* You need good reasoning without very high cost

### Opus

Opus is useful when:

* The task is complex
* You need deeper reasoning
* You are solving hard problems
* Quality matters more than speed or cost

---

## Real-World Lesson

In industry, model choice matters.

A beginner may think:

```text
Always use the biggest model.
```

But an AI engineer thinks:

```text
Use the cheapest model that gives good enough quality.
```

This project teaches that mindset.

---

## GitHub Commit

```bash
git add .
git commit -m "feat: add claude model comparison tool"
```

---

## Future Improvements

Possible upgrades:

* Save results to a CSV file
* Add cost calculation
* Add response quality score
* Add retry logic
* Add streaming responses
* Add menu to choose models
* Add JSON output
* Add web dashboard
* Add automated evaluation system

---

## Project Status

Completed Module 3:

```text
Choosing The Right Model
```

Built:

```text
Claude Model Comparison Tool
```

Learned:

```text
Cost optimization
Latency
Model selection
Token usage
API error handling
```
